# lnwjud installer resources

This directory is owned by the lnwjud project and is passed to electron-builder as `buildResources`.
The MVP installer uses electron-builder's generated application icon; no external fonts or remote assets are embedded.
